[pytest]
testpaths = tests
pythonpath = .
addopts = -p no:cacheprovider
timeout = 60
markers =
    live: makes real calls to Google, Travelpayouts or OpenAI; runs only with --live
filterwarnings =
    ignore::DeprecationWarning
